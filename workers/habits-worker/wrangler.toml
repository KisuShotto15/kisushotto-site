name = "habits-worker"
main = "src/index.js"
compatibility_date = "2025-04-10"

[[d1_databases]]
binding = "DB"
database_name = "habits-db"
database_id = "d15b535c-edda-4bf8-8805-1fc3c1c244ea"

[[kv_namespaces]]
binding = "PUSH_KV"
id = "183e28e496f3468fae40a58237cb098f"

[triggers]
crons = ["* * * * *"]

[vars]
VAPID_SUBJECT = "mailto:efrenalejandro2010@gmail.com"
# Quien puede entrar. VACIA = NO PASA NADIE (default seguro: es una app personal).
ALLOWED_EMAILS = "kisushotto.btc@proton.me"

# TOKEN ya no existe: la identidad sale del JWT del sitio. Estaba aqui en texto
# plano Y en el bundle del navegador, asi que no protegia nada.
#
# Set as secrets via:
#   wrangler secret put JWT_SECRET       <- el mismo valor que en Vercel
#   wrangler secret put VAPID_PUBLIC
#   wrangler secret put VAPID_PRIVATE_JWK
